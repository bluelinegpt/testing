import { apiBase } from './api-base';

export interface DemoRequestPayload { companyName:string;contactPerson:string;mobileNumber:string;email:string;country:string;emirate?:string|undefined;approximateDriverCount?:number|undefined;approximateMonthlyOrders?:number|undefined;preferredContactMethod:string;mainChallenges?:string|undefined;featuresOfInterest?:string[]|undefined;consent:true;companyFax?:string|undefined;landingPage:string;source?:'public_website'|'contact_page'|undefined;referrer?:string|undefined;utmSource?:string|undefined;utmMedium?:string|undefined;utmCampaign?:string|undefined;utmTerm?:string|undefined;utmContent?:string|undefined;gclid?:string|undefined; }
export class DemoRequestError extends Error { public constructor(message:string,public readonly status:number){super(message);} }
export async function submitDemoRequest(payload:DemoRequestPayload):Promise<{id:string;referenceNumber:string}>{ const response=await fetch(`${apiBase()}/public/demo-requests`,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(payload)}); if(!response.ok){let message=response.status>=500?"We couldn't submit your request right now. Please try again.":'We could not submit your request. Please review the form and try again.'; try{const body=await response.json() as {error?:{message?:string|string[]};message?:string|string[]}; const safe=body.error?.message??body.message; if(Array.isArray(safe)&&safe.length) message=safe[0]??message; else if(typeof safe==='string'&&safe.trim()) message=safe;}catch{/* Safe fallback. */} throw new DemoRequestError(message,response.status);} return await response.json() as {id:string;referenceNumber:string}; }
