import { Body,Controller,Get,HttpCode,Inject,Param,Patch,Post,Query } from "@nestjs/common";
// eslint-disable-next-line @typescript-eslint/consistent-type-imports
import { TraderApplicationListQueryDto,TraderApplicationNoteDto,TraderApplicationResolutionDto,TraderApplicationStatusDto } from "../trader-applications/trader-application.dto.js";
// eslint-disable-next-line @typescript-eslint/consistent-type-imports
import { TraderApplicationService } from "../trader-applications/trader-application.service.js";
// eslint-disable-next-line @typescript-eslint/consistent-type-imports
import { IdentityContextAccessor } from "../security/identity-context.js"; import { PLATFORM_TRADER_APPLICATIONS_MANAGE,PLATFORM_TRADER_APPLICATIONS_READ,RequirePlatformPermissions } from "./platform-authorization.js";
@Controller("platform/trader-applications") export class PlatformTraderApplicationController{constructor(@Inject(TraderApplicationService) private readonly service:TraderApplicationService,@Inject(IdentityContextAccessor) private readonly identity:IdentityContextAccessor){}private actor(){return this.identity.current().identityId;} @RequirePlatformPermissions(PLATFORM_TRADER_APPLICATIONS_READ) @Get() list(@Query()q:TraderApplicationListQueryDto){return this.service.list({...q,page:q.page??1,pageSize:q.pageSize??25,sort:q.sort??"newest"});} @RequirePlatformPermissions(PLATFORM_TRADER_APPLICATIONS_READ) @Get(":id") detail(@Param("id")id:string){return this.service.detail(id);} @RequirePlatformPermissions(PLATFORM_TRADER_APPLICATIONS_MANAGE) @Patch(":id/status") status(@Param("id")id:string,@Body()b:TraderApplicationStatusDto){return this.service.transition(id,b.status,b.reason,this.actor());} @RequirePlatformPermissions(PLATFORM_TRADER_APPLICATIONS_MANAGE) @HttpCode(201) @Post(":id/notes") note(@Param("id")id:string,@Body()b:TraderApplicationNoteDto){return this.service.addNote(id,b.text,this.actor());} @RequirePlatformPermissions(PLATFORM_TRADER_APPLICATIONS_MANAGE) @Patch(":id/delivery-company-resolution") resolve(@Param("id")id:string,@Body()b:TraderApplicationResolutionDto){return this.service.resolve(id,b.companyId,b.resolution,this.actor());}}
